<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Update Here..</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center">

    <div class="w-full max-w-3xl bg-zinc-800 rounded-xl shadow-lg p-10">

        <!-- Navigation -->
        <div class="flex justify-between items-center mb-8">
            <h1 class="text-4xl font-bold">Update Wallpaper details</h1>
            <a class="text-blue-400 hover:text-blue-600 transition duration-300" href="/read">🔙 Back to Homepage</a>
        </div>

        <!-- Update Form -->
        <form action="/update/<%= user._id %>" method="post" class="space-y-6">

            <!-- Name -->
            <div>
                <label class="block text-lg mb-2">Name</label>
                <input 
                    class="w-full px-5 py-3 border-2 border-zinc-600 bg-zinc-700 outline-none rounded-lg focus:ring-2 focus:ring-blue-500 transition" 
                    type="text" name="name" value="<%= user.name %>" placeholder="Enter name" required>
            </div>

            <!-- Email -->
            <div>
                <label class="block text-lg mb-2">Email</label>
                <input 
                    class="w-full px-5 py-3 border-2 border-zinc-600 bg-zinc-700 outline-none rounded-lg focus:ring-2 focus:ring-blue-500 transition" 
                    type="email" name="email" value="<%= user.email %>" placeholder="Enter email" required>
            </div>

            <!-- Image URL -->
            <div>
                <label class="block text-lg mb-2">Image URL</label>
                <input 
                    class="w-full px-5 py-3 border-2 border-zinc-600 bg-zinc-700 outline-none rounded-lg focus:ring-2 focus:ring-blue-500 transition" 
                    type="url" name="image" value="<%= user.image %>" placeholder="Paste image URL" required>
            </div>

            <!-- Submit Button -->
            <div class="flex justify-end">
                <input 
                    class="px-6 py-3 bg-yellow-500 text-black rounded-lg hover:bg-yellow-600 transition cursor-pointer" 
                    type="submit" value="✅ Update Wallpaper">
            </div>

        </form>

    </div>

</body>
</html>
